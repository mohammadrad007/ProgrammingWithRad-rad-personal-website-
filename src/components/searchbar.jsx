import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="جستجو"
      className="form-control form-control-dark form-bar"
    />
  );
};

export default SearchBar;
